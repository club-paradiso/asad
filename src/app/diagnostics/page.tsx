import { DiagnosticsScreen } from "@/features/diagnostics/DiagnosticsScreen";

export const metadata = { title: "Diagnostics — tong-yuck" };

export default function DiagnosticsPage() {
  return <DiagnosticsScreen />;
}
