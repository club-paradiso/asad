node_modules/
.next/
out/
build/
dist/
coverage/
*.tsbuildinfo
next-env.d.ts

.env
.env.local
.env.*.local

.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel

# The seeded canvas is a build output of .design-canvas/*.dc.html — it embeds a
# ~2MB editor payload and is republished from source, never edited in place.
.design-canvas/*.html
.design-canvas/**/*.html
!.design-canvas/*.dc.html
!.design-canvas/**/*.dc.html
# Seeded canvases are written to the repo root before publishing.
/asad-*.html

# Output of the e2e scripts and `npm run shot`. CI writes these on every run
# and uploads them as an artifact; they are a build product, not source.
e2e-out/
shot.png
